const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.08,rootMargin:'0px 0px -45px'});$$('.reveal').forEach(e=>observer.observe(e));setTimeout(()=>$$('.reveal').forEach(e=>e.classList.add('visible')),1200);
const cursor=$('#cursor');window.addEventListener('pointermove',e=>{if(cursor){cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'}},{passive:true});
const progress=$('#progress');window.addEventListener('scroll',()=>{const d=document.documentElement;progress.style.width=((d.scrollTop/(d.scrollHeight-d.clientHeight))*100)+'%';let current='home';$$('main section[id]').forEach(s=>{if(scrollY>=s.offsetTop-180)current=s.id});$$('nav a').forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+current))},{passive:true});
$$('.tilt').forEach(card=>{card.addEventListener('pointermove',e=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(1100px) rotateX(${(-y*7).toFixed(2)}deg) rotateY(${(x*8).toFixed(2)}deg) translateY(-4px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});
$$('.magnetic').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.transform=`translate(${((e.clientX-r.left-r.width/2)*.12).toFixed(1)}px,${((e.clientY-r.top-r.height/2)*.12).toFixed(1)}px)`});el.addEventListener('pointerleave',()=>el.style.transform='')});
const canvas=$('#particles'),ctx=canvas.getContext('2d');let dots=[];function resize(){canvas.width=innerWidth;canvas.height=innerHeight;dots=Array.from({length:Math.min(100,Math.floor(innerWidth/14))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.4+.3,v:Math.random()*.18+.04,p:Math.random()*Math.PI*2}))}function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);for(const d of dots){d.y-=d.v;if(d.y<0)d.y=canvas.height;const a=.18+.12*Math.sin(Date.now()/1200+d.p);ctx.beginPath();ctx.arc(d.x,d.y,d.r,0,Math.PI*2);ctx.fillStyle=`rgba(215,255,67,${a})`;ctx.fill()}requestAnimationFrame(draw)}resize();addEventListener('resize',resize);draw();
const form=$('#contactForm'),status=$('#formStatus');form?.addEventListener('submit',async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form).entries());status.textContent='Sending…';try{const res=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const json=await res.json();if(!res.ok)throw Error(json.error||'Failed');status.textContent='Message received — thank you.';form.reset()}catch(err){status.textContent='Backend is not running. Email me directly instead.'}});
